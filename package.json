{
  "name": "viva-agent",
  "version": "0.1.0",
  "description": "AI mock interviewer agent — a thin agent loop built on pi-ai, inspired by the pi coding agent's minimalist philosophy",
  "type": "module",
  "bin": {
    "viva": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js",
    "test": "npm run build && node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@earendil-works/pi-ai": "^0.79.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
